<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" type="text/css">
        <title>Contacts List</title>
    </head>
    <body>
        <div class="container">

            <h1>Contacts List</h1>

            {% if contacts is not empty %}
            <p>Here are all your contacts:</p>
                {% for contact in contacts %}
                <ul>
                    <li><strong>Name</strong>: {{ contact.showFullName }}</li>
                    <li><strong>Phone Number</strong>: {{ contact.getPhoneNumber }}</li>
                    <li><strong>Address</strong>: {{ contact.getAddress }}</li>
                </ul>
                {% endfor %}
            {% endif %}

            <h2>Please enter a contact to add to your contacts list:</h2>
            <form action="/create_contact" method="post">
                <div class="form-group">
                    <label for="first_name">First Name:</label>
                    <input id="first_name" class="form-control" name="first_name" type="text">
                </div>
                <div class="form-group">
                    <label for="last_name">Last Name:</label>
                    <input id="last_name" class="form-control" name="last_name" type="text">
                </div>
                <div class="form-group">
                    <label for="phone_number">Phone number:</label>
                    <input id="phone_number" class="form-control" name="phone_number" type="text">
                </div>
                <div class="form-group">
                    <label for="address">Street Address:</label>
                    <input id="address" class="form-control" name="address" type="text">
                </div>
                <button class="btn btn-info" type="submit" name="add_new_contact_clicked">Add a Contact!</button>
            </form>
            <br>
            <form action="/delete_contact" method="post">
                <button class="btn btn-danger" type="submit" name="delete_all_contacts_button_clicked">Clear All Contacts</button>
            </form>
        </div>
    </body>
</html>
